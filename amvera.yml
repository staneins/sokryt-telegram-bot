meta:
  environment: jvm
  toolchain:
    name: maven
    version: 17
build:
  artifacts:
    target/*.jar: /
  args: spring-boot:repackage
run:
  jarName: SokrytBot-1.0-SNAPSHOT.jar
  persistenceMount: /data
  containerPort: "8080"
